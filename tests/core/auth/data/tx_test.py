from terra_sdk.core import Tx

# def test_deserializes_tx(load_json_examples):
#     data = load_json_examples("./Tx.data.json")
#     example = data["tx"]
#     parsed = Tx.from_data(example).to_data()
#     for key in parsed.keys():
#         assert parsed[key] == example[key]
